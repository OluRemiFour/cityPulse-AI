// MapViewPage.tsx — full-screen map view using live complaint pins
// This is a page-level wrapper around HotspotMap
import HotspotMap from '../components/HotspotMap';

export default function MapViewPage() {
  return <HotspotMap />;
}
